services:
  - type: web
    name: student-performance-prediction
    runtime: python
    plan: free

    buildCommand: |
      pip install -r requirements.txt

    startCommand: |
      cd backend && gunicorn app:app
